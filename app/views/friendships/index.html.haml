%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @friendships.each do |friendship|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(friendship.email_address.downcase)}?s=40")
            = link_to friendship.full_name, friendship
          %td.extra-padding= friendship.queue_items.count
          %td.extra-padding= friendship.people_following_them.count
          %td.extra-padding
            = link_to friendship_path(friendship), { method: :delete, data: { confirm: "Are you sure?" } } do
              %i.glyphicon.glyphicon-remove
